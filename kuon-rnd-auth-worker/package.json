{
  "name": "kuon-rnd-auth-worker",
  "version": "1.0.0",
  "description": "Authentication worker for Kuon R&D — magic link, JWT, user management",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml"
  },
  "type": "commonjs",
  "dependencies": {
    "hono": "^4.12.12",
    "stripe": "^22.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^6.0.2",
    "wrangler": "^4.81.1"
  }
}
